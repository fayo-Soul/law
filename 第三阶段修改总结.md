# 律所法律知识库 RAG 系统：第三阶段修改总结

## 1. 阶段目标

第三阶段不继续扩张低质量数据量，而是围绕律师实际业务重建一个“小而准”的精选法律知识库，解决第二阶段发现的历史数据乱码、元数据缺失、关键法条漏召回和案例污染问题。

本阶段验收目标：

- 精选数据结构完整率 100%。
- 10 条核心业务问题平均 Recall@K 不低于 80%。
- 检索 P95 不超过 5 秒。
- 每个问题必须返回法律依据。
- 生成式抽样回答关键法条召回率和引用覆盖率不低于 80%。
- 默认回答不混入尚未治理的历史案例。

## 2. 业务范围

首批只覆盖高频、规则相对明确、便于验证的业务：

| 领域 | 业务问题 |
| --- | --- |
| 婚姻家事 | 离婚子女直接抚养、抚养费 |
| 民事争议 | 三年诉讼时效及起算 |
| 合同业务 | 不履行合同的违约责任、违约金调整 |
| 劳动争议 | 违法解除、经济补偿、即时解除、无过失解除、未签合同双倍工资、仲裁时效 |
| 交通行政 | 无证驾驶罚款和拘留 |

项目没有用少量数据假装覆盖全部中国法律。当前系统只对以上场景具备经过第三阶段评测的业务质量保证。

## 3. 数据重建

### 3.1 新建精选法规数据

文件：

```text
data/curated/legal_knowledge_v1.jsonl
```

共 12 条，涉及：

- 《中华人民共和国民法典》第一百八十八条、第五百七十七条、第五百八十五条、第一千零八十四条、第一千零八十五条。
- 《中华人民共和国劳动合同法》第三十九条、第四十条、第四十七条、第八十二条、第八十七条。
- 《中华人民共和国劳动争议调解仲裁法》第二十七条。
- 《中华人民共和国道路交通安全法》第九十九条。

### 3.2 数据结构

每条数据包含：

- 稳定文档 ID。
- 法律完整名称。
- 具体条号。
- 法律领域。
- 文档类型。
- 版本。
- 公布日期和施行日期。
- 效力状态。
- 权威来源地址。
- 条文原文。
- 业务场景关键词。
- 律师业务适用提示。

### 3.3 修改原因

历史数据只有模糊领域名，标题出现乱码，缺少条号、版本和效力状态，导致模型虽然能生成回答，却无法召回评测指定的关键依据。

精选数据将“检索文本”和“回答证据”分开：

- 检索文本包含法律名称、条号、业务关键词和业务适用语义。
- 返回给模型的父块明确区分“条文原文”和“业务适用”。
- 引用仍定位到法律名称和具体条号，不把业务总结伪装成法条原文。

## 4. 独立精选 Milvus Collection

### 修改点

- 新建 Collection：`fzt_legal_curated_v1`。
- `.env`、`.env.example` 和 `config.ini` 默认切换到精选 Collection。
- 历史 `fzt_legal_articles` Collection 不删除，可用于回滚和后续治理。
- 新增 `scripts/ingest_curated_knowledge.py`。

### 入库器能力

- 校验必填字段。
- 拒绝同一法律、条号和版本的重复记录。
- 生成稳定 `document_id` 和 `chunk_id`。
- 写入 dense vector 和 sparse vector。
- 写入法律名称、条号、来源、版本、效力状态、来源 URL、施行日期和业务摘要。
- 支持删除并重建 Collection，也支持增量 upsert。

### 执行结果

```text
collection=fzt_legal_curated_v1
records=12
```

12 条精选数据全部完成 BGE-M3 向量化并写入 VMware Milvus。

## 5. 检索与引用改造

### 修改点

- Milvus 输出字段新增：
  - `source_url`
  - `effective_date`
  - `promulgation_date`
  - `business_summary`
- 引用 metadata 增加权威来源 URL、公布日期和施行日期。
- 默认检索范围从“法规、司法解释和案例”改为“法规与司法解释”。
- 案例检索保留，但必须由用户明确选择。
- 前端默认选项改为“法规与司法解释”。

### 修改原因

旧案例集合尚未完成稳定哈希重建、标题修复和业务评测。默认混入 5 条案例会稀释精选法规依据，增加模型上下文和生成延迟。

### 成果

劳动违法解除问题只返回：

- 《中华人民共和国劳动合同法》第八十七条。
- 《中华人民共和国劳动合同法》第四十七条。

不再默认混入无关案例。

## 6. 业务评测集

文件：

```text
data/eval/curated_business_v1.jsonl
```

共 10 条业务问题，每条明确配置应召回的法律名称和条号，覆盖全部首批业务范围。

评测问题使用自然语言表达，没有直接把标准答案拼进问题，也没有使用隐藏标记操纵向量检索。

## 7. 检索质量门禁

新增：

```text
scripts/evaluate_retrieval.py
```

该工具不调用大模型，只评估 RAG 的 Retrieval 部分，避免将模型生成延迟误算为检索延迟。

指标：

- 平均 Recall@K。
- P50 检索延迟。
- P95 检索延迟。
- 是否每题均返回依据。

默认门槛：

- 平均 Recall@K ≥ 80%。
- P95 ≤ 5,000 ms。
- 每题都有检索证据。

### 实测结果

```text
样本数：10
平均 Recall@K：100%
P50：1,448.0 ms
P95：1,890.1 ms
每题返回依据：通过
总体：通过
```

报告：

```text
data/eval/curated_retrieval_report.json
```

与第二阶段历史数据抽样 Recall@K 为 0% 相比，精选业务数据的关键依据召回提升到 100%。

## 8. 生成式回答抽样验收

使用精选评测集前 3 条进行带大模型回答验收：

```text
成功执行：3/3
Recall@K：100%
引用覆盖率：100%
平均回答耗时：22,773.4 ms
运行错误：0
```

报告：

```text
data/eval/curated_answer_report.json
```

结果证明精选知识库可以支持结构化回答和准确条款引用。回答平均耗时仍约 22.8 秒，主要耗时来自外部大模型生成，不是 Milvus 检索；后续应通过流式输出、模型选择和缓存继续优化体验。

## 9. 自动化测试

新增 `tests/unit/test_phase3_curated_knowledge.py`，验证：

- 精选数据至少 10 条。
- 效力状态和权威来源地址完整。
- 文档 ID 唯一。
- 法律名称和条号正确进入引用元数据。
- 父块同时包含条文原文和业务适用。
- 入库器拒绝字段不完整的数据。

最终结果：

```text
60 passed, 8 warnings
```

8 条 warning 来自第三方 `pkg_resources` 弃用提示。

## 10. 权威来源

首批数据根据国家机关和司法机关公开文本核对，主要包括：

- 最高人民法院公开的《中华人民共和国民法典》全文。
- 中国政府网公开的《中华人民共和国劳动合同法》。
- 最高人民法院公报公开的《中华人民共和国劳动争议调解仲裁法》。
- 政府网站公开的现行《中华人民共和国道路交通安全法》。

每条 JSONL 数据保存了对应 `source_url`。维护人员更新法条时必须回到权威原文复核，不能将搜索摘要或 AI 生成文本作为法条原文。

## 11. 主要修改文件

- `data/curated/legal_knowledge_v1.jsonl`
- `data/curated/README.md`
- `data/eval/curated_business_v1.jsonl`
- `data/eval/curated_retrieval_report.json`
- `data/eval/curated_answer_report.json`
- `scripts/ingest_curated_knowledge.py`
- `scripts/evaluate_retrieval.py`
- `scripts/evaluate_rag.py`
- `rag_qa/core/vector_store.py`
- `app/citations.py`
- `api_server.py`
- `main.py`
- `web/index.html`
- `tests/unit/test_phase3_curated_knowledge.py`
- `tests/integration/test_legal_rag_api_contract.py`
- `.env`
- `.env.example`
- `config.ini`
- `README.md`
- `pyproject.toml`
- `uv.lock`

## 12. 当前业务边界

### 已经过评测

- 离婚子女抚养和抚养费。
- 民事普通诉讼时效。
- 合同违约责任和违约金调整。
- 劳动违法解除赔偿、经济补偿、未签合同双倍工资和仲裁时效。
- 无证驾驶行政处罚。

### 尚未经过评测

- 刑事辩护。
- 公司治理和股权争议。
- 建设工程和房地产。
- 知识产权。
- 税务和金融监管。
- 数据合规。
- 破产重整。
- 全量司法解释。
- 案例类比和裁判观点。
- 地方法规及地方裁量基准。

对于未覆盖领域，系统可能只返回“当前知识库证据不足”，不能把首批 12 条数据的 100% Recall 外推成全法律领域准确率。

## 13. 第三阶段结论

第三阶段已经完成“小而准、以业务为主”的数据重建：

- 历史脏法规集合不再作为默认知识库。
- 12 条核心条款具有完整版本、效力和来源元数据。
- 10 条业务问题检索 Recall@K 达到 100%。
- 检索 P95 低于 2 秒。
- 生成式抽样回答关键法条召回和引用覆盖均为 100%。
- 默认回答不再混入未治理案例。

当前版本可以在明确标注业务范围的前提下，用于上述首批场景的受控内网试用。

正式扩大使用范围时，应按同样流程逐领域增加权威数据和评测问题；每扩展一个法律领域，都必须先通过数据审核、检索门禁和律师人工复核，不能重新回到“先导入大量数据、再期待模型自行解决质量”的方式。
