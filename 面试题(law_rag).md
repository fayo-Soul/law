# 法律咨询 RAG 项目面试题与解答

> 本项目定位：面向中文法律场景的智能问答系统（前端品牌“法智通”），以法律条文、司法解释、案例文书、结构化问答对为知识源，通过 RAG + LLM 生成可溯源、带免责声明的回答。

---

## 一、项目整体认知

### 1. 请用 2-3 分钟介绍一下这个法律咨询 RAG 项目。

#### 面试答案（日常化）

“这个项目是一个面向中文法律场景的智能问答系统，我们内部叫它‘法智通’。它的核心思路就是 RAG：把法律条文、司法解释、典型案例还有结构化问答对当成知识库，用户提一个法律问题后，系统先判断问题的领域，然后去查 MySQL 里的高频问答对、去 Milvus 里检索法律条文和案例，最后用通义千问或 DeepSeek 这样的大模型生成一个带引用来源、带免责声明的回答。

整体上是三层检索兜底：第一层是结构化问答对，命中了就直接返回答案，速度最快；第二层是法律条文的向量检索；第三层是案例检索，用来补充实际判例。然后通过 Prompt 控制模型不能编法条、不能给具体法律建议，最后会引导用户咨询专业律师。”

#### 知识点讲解

- **RAG（Retrieval-Augmented Generation）**：检索增强生成，解决大模型“幻觉”问题，让模型基于真实文档作答。
- **三层检索架构**：高频问答对（快）→ 法律条文（准）→ 案例（深），兼顾响应速度和回答深度。
- **可溯源性**：返回 `references` 字段，标明引用类型（article/case）、标题、来源和相似度得分。

---

## 二、RAG 核心流程

### 2. 用户提了一个问题后，系统内部是怎么处理的？请描述完整链路。

#### 面试答案（日常化）

“用户输入问题后，首先会做一次敏感词检测，比如涉及自杀、家暴、死刑、未成年人这些词会先记日志。然后系统会判断这是不是法律问题。

如果是普通闲聊，就直接让大模型回答，不走 RAG。如果是法律问题，就先走一个 BERT 分类器判断属于刑法、民法、劳动法、行政法还是其他法。

接下来先查 MySQL 里的结构化问答对，用 BM25 算相似度，同时会看 Redis 里有没有缓存。如果命中了且分数够高，直接返回答案。

如果没命中，就进入 RAG：先去 Milvus 里用 BGE-M3 做稠密+稀疏混合检索法律条文，召回子块之后再向上聚合到父块，然后用 BGE-Reranker 精排。这里还会根据问题类型选择检索策略，比如直接检索、HyDE、子查询或者回溯。

法律条文检索完之后，再用条文+问题去检索相似案例。最后把条文和案例拼成上下文，送进大模型生成带引用来源的回答。”

#### 知识点讲解

1. **敏感词检测**：对高风险法律问题打标，便于后续审计或人工介入。
2. **法律领域分类**：`LawQueryClassifier` 使用 BERT 多标签分类 + 关键词规则兜底 + 非法律问题识别。
3. **结构化问答对检索**：`LawQASystem.query()` → Redis 缓存 → MySQL 查询 → BM25 相似度排序。
4. **RAG 检索**：
   - `StrategySelector` 选择检索策略（直接 / HyDE / 子查询 / 回溯）。
   - `VectorStore.hybrid_search_with_rerank()` 执行混合检索 + 重排序。
   - 父块聚合：子块用于精细召回，父块用于保证上下文完整。
5. **案例检索**：`LegalSearcher.search_by_articles()` 用“法律条文 + 用户问题”检索相关判例。
6. **答案生成**：使用 `RAGPrompt.law_rag_prompt()`，强制要求引用来源、禁止编造、添加免责声明。

---

## 三、向量检索与 Milvus

### 3. 为什么选用 Milvus？你们是怎么做向量检索的？

#### 面试答案（日常化）

“选用 Milvus 主要是因为它支持海量向量的高性能近似最近邻检索，而且原生支持 hybrid_search，可以同时做稠密向量和稀疏向量的混合检索。项目里法律条文和案例的数据量不小，Milvus 在可扩展性和检索效率上都比较合适。

具体检索时，我们用 BGE-M3 把文本编码成稠密向量（dense vector）和稀疏向量（sparse vector）。稠密向量捕捉语义，稀疏向量保留关键词信息。然后在 Milvus 里分别对两种向量做 ANN 检索，再用 WeightedRanker 把两个结果加权融合，得到综合得分。最后把候选文档交给 BGE-Reranker 做精排，提升相关性。”

#### 知识点讲解

- **Milvus 优势**：分布式、高吞吐、支持多种索引类型（IVF、HNSW）、支持多向量字段、支持 hybrid search。
- **稠密向量（Dense Vector）**：由 BGE-M3 dense 输出，语义相似度高，对同义词、改写敏感。
- **稀疏向量（Sparse Vector）**：由 BGE-M3 sparse 输出，类似词袋模型，对关键词匹配强。
- **混合检索融合**：`WeightedRanker` 对 dense 和 sparse 的检索结果按权重融合，兼顾语义和关键词。
- **重排序（Rerank）**：BGE-Reranker-Large 作为 Cross-Encoder，把 query 和 doc 拼起来打分，精度高但计算量大，所以只对 Top-K 候选做。

---

### 4. 什么是父子块（Parent-Child Chunking）？为什么要这样设计？

#### 面试答案（日常化）

“父子块就是我们切文档的时候切两种粒度。子块比较小，比如 128 tokens，用来生成向量做检索，这样匹配更精细；父块比较大，比如 512 tokens，用来保留完整语义，真正送给大模型当上下文。

这样做的好处是，检索的时候可以用小粒度命中用户问题的关键点，但给模型看的时候是大粒度的内容，避免断章取义。比如一条法律条文可能被切成几个子块，用户问题命中了其中一个子块，我们就会把整个父块，也就是整条或整段条文，一起送给模型。”

#### 知识点讲解

- **子块（Child Chunk）**：小粒度文本块，用于 embedding 和向量检索，提高召回精度。
- **父块（Parent Chunk）**：大粒度文本块，用于 LLM 上下文，保留完整语义。
- **`parent_id` / `parent_content`**：子块记录父块 ID 和内容，检索到子块后向上聚合父块。
- **优势**：避免大 chunk 检索粒度粗、小 chunk 上下文丢失的问题，平衡检索精度和生成质量。

---

## 四、检索策略与查询理解

### 5. 项目里有哪些检索策略？是怎么选择的？

#### 面试答案（日常化）

“项目里设计了四种检索策略：直接检索、HyDE、子查询和回溯。

选择策略是靠一个 `StrategySelector` 模块，它会把用户问题送给大模型，让模型判断这个问题适合哪种策略。比如简单明确的法条查询就直接检索；需要模型先假设答案再检索相似文档的用 HyDE；一个复杂问题拆成多个子问题分别检索的用子查询；用户问题有歧义或者需要澄清的用回溯策略。

这样设计是为了让检索更有针对性，不是所有问题都用同一种方式去查，提高召回率。”

#### 知识点讲解

- **直接检索（Direct Retrieval）**：对清晰、具体的法律问题直接检索。
- **HyDE（Hypothetical Document Embeddings）**：让 LLM 先生成假设答案，再用假设答案去检索，适合用户问题较短或表达不够规范的场景。
- **子查询（Sub-query）**：把复杂问题拆成多个子问题分别检索，适合多条件、多法律关系的咨询。
- **回溯（Step-back）**：对模糊问题先做抽象或澄清，再检索，适合需要引导用户补充信息的场景。

---

### 6. BERT 法律领域分类是怎么做的？分类结果有什么用？

#### 面试答案（日常化）

“我们用了一个自己微调的 BERT 分类器，叫 `bert_law_classifier`。它会判断问题属于刑法、民法、劳动法、行政法还是其他法，支持多标签，也就是一个问题可能同时属于多个领域。

为了让分类更稳，我们加了关键词规则兜底。比如问题里出现‘劳动合同’就优先判劳动法；出现‘杀人’‘盗窃’就优先判刑法。如果分类概率特别低，或者命中了非法律关键词，系统就认为是非法律问题，直接走大模型通用回答。

分类结果主要用来当 `source_filter`，过滤 Milvus 里的法律条文检索范围，避免全库乱搜，提高精度和速度。”

#### 知识点讲解

- **多标签分类（Multi-label）**：一个问题可能同时涉及多个法律领域。
- **规则兜底**：BERT 不是 100% 可靠，关键词规则可以提升稳定性和可解释性。
- **非法律问题识别**：设置概率阈值，低于阈值或命中非法律关键词时判为闲聊。
- **`source_filter` 过滤**：限制检索范围，减少无关召回，提升效率和准确率。

---

## 五、结构化问答对与缓存

### 7. 为什么单独设计 MySQL 结构化问答对？BM25 在这里起什么作用？

#### 面试答案（日常化）

“MySQL 里存的是结构化问答对，相当于一个高频 FAQ。用户很多问题是重复的，比如‘试用期被辞退有没有赔偿’这种，如果能在 MySQL 里直接命中，响应速度比走 RAG 快很多，成本也低。

BM25 是用来算用户问题和库中问题的文本相似度的。我们先对用户问题做 jieba 分词，然后和 MySQL 里问题的分词结果做 BM25 打分，再做一个 softmax 归一化，按阈值判断是否命中。命中的话直接返回答案，并且把结果缓存到 Redis，下次同样的问题直接从 Redis 拿。”

#### 知识点讲解

- **结构化问答对优势**：高频问题快速响应、降低 LLM 调用成本、答案质量稳定。
- **BM25**：经典概率检索模型，基于词频和逆文档频率计算相关性，适合短文本匹配。
- **jieba 分词**：中文分词工具，预处理后再做 BM25 匹配更准确。
- **Redis 缓存**：缓存命中问题和答案，进一步降低延迟和数据库压力。

---

## 六、案例检索

### 8. 案例检索和法律条文检索有什么区别？

#### 面试答案（日常化）

“法律条文和案例是分开存在两个 Milvus 数据库里的，一个叫 `fzt_law`，一个叫 `fzt_case`，物理隔离。

法律条文主要是法律法规、司法解释这些规范性文件，我们用了父子块切分。案例是裁判文书，字段更丰富，包括案号、法院、事实、裁判结果等，用的是滑窗切分。

检索流程上，案例检索会优先结合已经召回的法律条文去查。比如已经找到‘劳动合同法第 39 条’，就用‘劳动合同法第 39 条 + 用户问题’一起去检索相关案例。如果没找到法律条文，就只用用户问题做案例检索。”

#### 知识点讲解

- **双库设计**：法律条文和案例物理隔离，字段结构和检索目标不同。
- **案例字段**：案号、法院、原告/被告、事实、裁判理由、裁判结果等，结构化程度更高。
- **滑窗切分**：案例文本较长，用滑动窗口切分保持上下文连续性。
- **`search_by_articles`**：用法律条文增强案例检索，提升案例与用户问题的相关性。

---

## 七、大模型与提示工程

### 9. 怎么避免大模型在法律回答里“胡说八道”？

#### 面试答案（日常化）

“首先我们用 RAG，答案必须基于检索到的法律条文和案例，不是让模型凭空生成。其次 Prompt 里有明确要求：不能编造法条、不能编造案号、不能给出具体法律建议，只能做信息参考。

另外我们还加了免责声明，告诉用户回答仅供参考，具体问题要咨询专业律师，并且附了人工客服电话。对于敏感词问题，比如自杀、家暴，系统会记录日志，回答也会更谨慎。”

#### 知识点讲解

- **RAG 抑制幻觉**：让模型基于检索文档生成，减少无依据内容。
- **Prompt 约束**：
  - 禁止编造法条/案号
  - 禁止提供具体法律建议
  - 必须引用来源
  - 必须添加免责声明
- **引用来源结构化**：返回 `references` 列表，便于前端展示和人工复核。
- **合规声明**：`CUSTOMER_SERVICE_PHONE` 等配置引导用户人工咨询。

---

## 八、工程化与部署

### 10. 项目是怎么部署的？用到 Docker 吗？

#### 面试答案（日常化）

“项目用 Docker Compose 来跑基础设施，包括 MySQL、Redis、Milvus，Milvus 还依赖 etcd 和 MinIO。Python 应用服务目前是用 Dockerfile 单独构建镜像，默认启动 API 服务。

启动前会有一个 `check_services.py` 脚本检查所有依赖：配置文件、数据库连接、Redis、Milvus、模型文件、Python 包这些是不是都正常。另外还有个 `backup.py` 脚本可以做全量或增量备份，生成备份报告。”

#### 知识点讲解

- **Docker Compose**：编排 MySQL、Redis、Milvus（etcd + MinIO）等基础设施。
- **Dockerfile**：构建 `law-rag` 镜像，默认启动 FastAPI 服务。
- **启动前检查**：`scripts/check_services.py` 确保依赖就绪再启动。
- **备份恢复**：`scripts/backup.py` 支持全量/MySQL/数据文件备份。

---

## 九、安全与隐私

### 11. 项目里做了哪些安全和隐私保护措施？

#### 面试答案（日常化）

“安全方面，我们在日志里对身份证号、手机号、邮箱、API Key 做了脱敏处理，避免敏感信息泄露。配置文件里现在有些 API Key 和密码还是明文的，我们也规划了迁移到环境变量和 `.env` 里。

API 服务有 Bearer Token 认证，管理后台的 CRUD 接口和 PIPL 数据删除接口都需要 token。另外有敏感词检测，高风险问题会记录日志便于审计。 complies 要求下还提供了用户数据删除接口。”

#### 知识点讲解

- **日志脱敏**：`base/security.py` 对身份证、手机号、邮箱、API Key 脱敏。
- **配置安全**：配置优先级为 环境变量 > `.env` > `config.ini` > 默认值，建议生产环境用环境变量注入。
- **API 认证**：管理接口使用 Bearer Token。
- **PIPL 合规**：提供用户数据删除接口，满足个人信息保护法要求。
- **敏感词审计**：自杀、家暴、死刑、未成年人等关键词触发日志记录。

---

## 十、性能优化与评估

### 12. 你们怎么评估 RAG 的效果？有没有做过性能优化？

#### 面试答案（日常化）

“评估方面有个 `evaluate_rag.py` 脚本，会看 Recall@K、响应耗时、引用统计这些指标。我们也会人工抽查一些问题的回答，看引用来源是不是正确、有没有幻觉。

性能优化上，首先是三层检索兜底，高频问题走 MySQL+Redis，不走 LLM；其次是 Redis 缓存问答对和会话历史；然后是向量检索用 HNSW 索引加速；重排序只对 Top-K 候选做，避免计算量太大；最后父子块设计也让检索更精准，减少无效上下文。”

#### 知识点讲解

- **评估指标**：
  - `Recall@K`：Top-K 里是否包含相关文档
  - 响应耗时：各环节耗时统计
  - 引用统计：引用来源数量、类型分布
- **性能优化手段**：
  - 结构化问答对优先，降低 LLM 调用
  - Redis 多级缓存
  - Milvus HNSW 索引
  - Reranker 只对 Top-K 执行
  - 父子块提升检索精度

---

## 十一、开放性问题

### 13. 如果让你继续优化这个项目，你会从哪些方面入手？

#### 面试答案（日常化）

“我会从几个方向去考虑：

第一，检索效果上，可以引入查询改写和扩展，对用户口语化的问题做标准化；也可以做用户意图识别，区分是查法条、查案例还是求建议，走不同的检索流程。

第二，工程上，把 API Key 和密码全部迁移到环境变量或密钥管理系统，提升安全性；把 Python 应用服务也加到 Docker Compose 里，实现一键部署。

第三，评估体系上，可以建立标准的评测集，定期跑 eval，观察 Recall、MRR、回答满意度这些指标的变化。

第四，成本上，可以对不同场景选择不同大小的模型，简单问题用小模型，复杂问题才用大模型。

第五，合规上，继续完善日志审计、用户数据删除、回答免责声明这些。”

#### 知识点讲解

- **查询改写（Query Rewriting）**：把口语化、模糊的问题改写成更适合检索的形式。
- **意图识别**：区分“查法条”“查案例”“求建议”等意图，调用不同策略。
- **密钥管理**：迁移到 KMS、Vault 或环境变量。
- **评测集建设**：固定测试集 + 人工标注，持续追踪指标。
- **模型路由**：简单问题用轻量模型，复杂问题用强模型，平衡成本和效果。

---

## 附录：快速记忆口诀

| 模块 | 一句话总结 |
|---|---|
| 整体定位 | 法智通 = 法律 RAG 问答，可溯源、带免责声明 |
| 三层检索 | MySQL 问答对 → Milvus 法律条文 → Milvus 案例 |
| 向量模型 | BGE-M3 编码，稠密 + 稀疏双向量 |
| 精排 | BGE-Reranker 对 Top-K 候选交叉编码重排 |
| 切分策略 | 法律条文用父子块，案例用滑窗切分 |
| 查询分类 | BERT 多标签 + 关键词规则兜底 |
| 检索策略 | 直接 / HyDE / 子查询 / 回溯，由 LLM 选择 |
| 安全合规 | 日志脱敏、敏感词审计、免责声明、PIPL 删除接口 |

---

*本文件由项目结构梳理生成，用于面试复习与技术学习。*
