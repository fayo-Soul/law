# 律所法律知识库 RAG 系统：第一阶段修改总结

## 1. 阶段目标

第一阶段解决的是“企业应用基础是否可靠”的问题，重点不是继续增加页面功能，而是先补齐身份认证、资料权限、上传安全、检索边界、审计、限流、健康检查、配置管理和自动化测试。

完成本阶段后，项目已经从演示型 RAG 原型提升为具备企业应用基础控制的可运行版本，但仍不能直接等同于已经完成生产验收。正式上线前还需要完成第二阶段的持久化任务队列、统一身份系统、监控告警、备份恢复、安全扫描和压力测试。

## 2. 修改点、修改原因和技术实现

### 2.1 增加查询意图路由

**修改点**

- 新增 `app/intent.py`。
- 将问题划分为 `product_help`、`greeting`、`courtesy`、`out_of_scope` 和 `legal_research`。
- 系统介绍、问候、致谢和明显非法律问题直接返回固定边界回答，不再检索法规并生成虚假法律分析。
- 法律问题仍进入混合检索、重排序和带引用回答流程。

**修改原因**

原系统把“这个系统能帮我查询什么”等日常问题也当作法律问题，可能检索到无关法条并形成错误引用。企业系统必须先判断问题是否属于法律研究范围。

**技术点**

- 轻量规则意图路由。
- RAG 前置分流。
- 确定性产品说明回答。
- 非法律问题不调用向量库和大模型，减少误答、延迟和模型费用。

**修改成果**

实测能力问题返回 `intent=product_help`，引用数量为 0，不再产生无关法律依据。

### 2.2 增加管理员和研究员两类身份

**修改点**

- 新增 `app/auth.py`。
- 支持管理员令牌 `API_TOKEN` 和研究员令牌 `RESEARCHER_API_TOKEN`。
- 管理员可执行文档上传、查看、重建索引、删除等管理操作。
- 研究员可查询内部资料，但不能调用管理接口。
- 未认证用户只能查询公共法律资料。
- 删除了“未配置令牌时自动放行”的不安全行为。

**修改原因**

原接口只有简单共享令牌，而且空令牌可能绕过保护，无法区分管理员和普通研究人员。律所内部资料必须有明确的访问边界。

**技术点**

- HTTP Bearer Token。
- 基于角色的访问控制（RBAC）。
- `hmac.compare_digest` 常量时间令牌比较。
- FastAPI `Depends` 统一鉴权依赖。
- 管理员和研究员使用不同凭据。

**修改成果**

- 匿名访问内部检索接口返回 401。
- 研究员访问内部检索接口返回 200。
- 匿名访问文档管理接口返回 401。
- 管理员访问文档管理接口返回 200。

### 2.3 增加文档级访问范围过滤

**修改点**

- 文档增加 `access_level` 元数据，支持 `internal` 和 `restricted`。
- 研究员只能检索 `internal` 文档。
- 管理员可以检索 `internal` 和 `restricted` 文档。
- `access_level` 写入 Milvus 动态字段并参与检索表达式过滤。
- 默认公共检索范围不再包含内部资料。
- 前端增加内部资料访问令牌输入框，令牌只保存在当前页面会话中。

**修改原因**

只保护上传接口不够。如果检索阶段不按权限过滤，内部资料仍可能进入模型上下文并泄露给无权限用户。

**技术点**

- Milvus metadata filter。
- 检索前权限收敛。
- 公共范围和内部范围显式分离。
- 管理权限与检索权限分离。

**修改成果**

内部资料是否参与召回由服务端权限决定，不能由前端自行绕过。

### 2.4 加强上传文件安全

**修改点**

- 限制文件类型为 PDF、DOCX、TXT 和 Markdown。
- 拒绝 `../` 等路径穿越文件名。
- 默认限制单文件最大 20 MB，可通过 `MAX_UPLOAD_MB` 调整。
- 拒绝空文件和重复文件。
- 使用 SHA-256 判断重复文件。
- 检查 PDF `%PDF` 文件头、DOCX ZIP 文件头和文本 UTF-8 编码。
- 索引完成或失败后自动清理 staging 临时目录。
- 注册表损坏时明确报错，不再静默当作空知识库。

**修改原因**

只检查扩展名不能阻止伪造文件；路径穿越、无限制上传和临时文件残留都会形成安全与磁盘风险。注册表损坏后静默返回空列表还可能造成数据误判。

**技术点**

- 文件名规范化。
- 文件签名校验。
- 上传大小限制。
- SHA-256 内容摘要。
- 原子替换保存文档注册表。
- 临时目录生命周期管理。

**修改成果**

伪造 PDF、伪造 DOCX、非法编码文本、路径穿越和超限文件均会被拒绝。

### 2.5 修复案例稀疏检索哈希不稳定

**修改点**

- 新增 `case_rag/sparse.py`。
- 案例入库和查询统一使用 BLAKE2b 生成稳定 token ID。
- 移除 Python 内置 `hash()` 作为稀疏向量维度编号的做法。

**修改原因**

Python 内置 `hash()` 在不同进程中可能产生不同结果。入库进程和查询进程编号不一致时，BM25/稀疏检索结果会失真。

**技术点**

- BLAKE2b 稳定摘要。
- 固定稀疏向量空间映射。
- 入库端与查询端算法一致。

**修改成果**

相同词在不同运行进程中得到相同 token ID。已有案例集合需要在部署窗口执行一次重新入库，才能让历史稀疏向量全部切换到新算法。

### 2.6 增加检索过滤和引用边界

**修改点**

- `/api/v1/search` 和 `/api/v1/ask` 支持结构化 `filters`。
- 当前支持法律效力状态 `effective_status` 过滤。
- 过滤模型禁止未声明字段，避免任意表达式进入 Milvus。
- 默认只查询法规、司法解释和案例，不默认查询内部资料。
- 自动分类只作为提示，不再错误地把分类标签强制当作检索范围。
- 回答继续输出法规、案例、文件来源和定位信息。

**修改原因**

企业法律检索必须区分现行、修订、废止等状态，同时防止用户直接拼接数据库过滤表达式。错误的自动分类硬过滤还会漏掉跨领域法律依据。

**技术点**

- Pydantic 严格请求模型。
- 白名单过滤字段。
- Milvus 服务端过滤。
- 混合检索与 rerank。
- 引用来源追踪。

**修改成果**

公共检索实测成功返回 5 条结果，并可以按效力状态过滤。

### 2.7 增加审计日志

**修改点**

- 新增 `app/audit.py`。
- 记录检索、问答、上传、重建索引和删除操作。
- 审计字段包括时间、动作、操作者、目标、trace ID 和结果。
- 审计日志不记录用户问题正文和文档正文。

**修改原因**

企业系统需要知道谁在什么时间执行了什么操作，同时应避免审计日志自身成为敏感法律资料泄露点。

**技术点**

- JSON Lines 结构化日志。
- 独立审计文件 `logs/audit.jsonl`。
- 线程锁保证单进程并发写入完整性。
- 最小化敏感数据记录。

**修改成果**

关键读写操作具备基础追踪能力，可通过 trace ID 对照应用错误日志。

### 2.8 增加限流、错误响应和真实就绪检查

**修改点**

- 增加按客户端 IP 的每分钟请求限制。
- CORS 从全开放改为配置白名单。
- `/api/v1/health` 用于进程存活检查。
- `/api/v1/ready` 实际探测 MySQL、Redis、Milvus 端口和 RAG 初始化状态。
- 依赖不可用时 `/ready` 返回 503。
- 文档重建索引接口改为 202 异步接受，并维护 `queued/indexing/indexed/failed` 状态。

**修改原因**

原健康接口即使依赖故障也可能显示正常，无法用于负载均衡和运维判断。同步索引会长时间占用 HTTP 请求，请求限流和 CORS 全开放也不符合企业接口要求。

**技术点**

- FastAPI 中间件。
- 固定窗口内存限流。
- TCP 依赖探针。
- BackgroundTasks。
- 文档状态机。
- HTTP 202、401、403、503 等明确状态码。

**修改成果**

VMware 环境实测 MySQL、Redis、Milvus 和 RAG 四项依赖均为 `true`，`/ready` 返回 200。

### 2.9 清理配置中的明文秘密

**修改点**

- `config.ini` 改为只保存非敏感默认值。
- 数据库密码、Redis 密码、模型 API Key 和访问令牌统一从 `.env` 或部署环境变量读取。
- `.env.example` 只保留占位值。
- 增加 CORS、研究员令牌、限流和上传大小配置。

**修改原因**

项目配置文件曾包含真实连接信息和 API Key。秘密进入代码仓库后很难彻底撤回，也容易在分享项目时泄露。

**技术点**

- 环境变量优先级高于 INI。
- 配置与秘密分离。
- `.env` 被 Git 忽略。
- 管理员和研究员凭据分离。

**修改成果**

可共享配置文件不再保存真实凭据。曾经暴露过的 API Key 和密码仍应在对应平台执行轮换，删除文件中的内容不能使旧凭据自动失效。

### 2.10 统一依赖声明

**修改点**

- 修复 `pyproject.toml` 中空依赖和错误项目描述。
- 项目名称更新为 `lawfirm-legal-knowledge-rag`，版本更新为 `0.3.0`。
- 写入 FastAPI、Milvus、模型、LangChain、MySQL、Redis 和 OpenAI 兼容客户端等核心直接依赖。
- 重新生成 `uv.lock`，与项目元数据保持一致。

**修改原因**

原 `requirements.txt` 有完整依赖，但 `pyproject.toml` 声明为空，旧锁文件版本也不一致，可能造成开发环境能运行、部署环境无法复现。

**技术点**

- PEP 621 项目元数据。
- 直接依赖显式声明。
- uv 依赖锁定。
- Python 版本限制为 3.10 至 3.12。

**修改成果**

项目依赖可以通过 `uv` 按锁文件复现。旧 `.venv` 是失效的 uv 跳板环境，应在后续部署时删除后依据新锁文件重建。

## 3. 测试与验收结果

### 3.1 自动化测试

执行结果：

```text
53 passed, 8 warnings
```

覆盖内容包括：

- API v1 契约和状态码。
- 健康检查与认证边界。
- 文档完整生命周期。
- 重复文件、非法类型、路径穿越和大小限制。
- 伪造 PDF、伪造 DOCX 和非法文本编码。
- 管理员与研究员角色区分。
- 日常问题与法律问题意图路由。
- 案例 token ID 稳定性。
- 引用和检索响应结构。

8 条 warning 来自第三方包 `pkg_resources` 的弃用提示，不影响当前测试结果，但应在后续依赖升级阶段处理。

### 3.2 VMware 端到端验收

依赖服务器：`192.168.88.100`

| 验收项 | 结果 |
| --- | --- |
| MySQL 连通 | 通过 |
| Redis 连通 | 通过 |
| Milvus 连通 | 通过 |
| RAG 初始化 | 通过 |
| 能力问题不进入 RAG | 通过 |
| 公共法律检索 | 通过，返回 5 条结果 |
| 匿名查询内部资料 | 通过，返回 401 |
| 研究员查询内部资料 | 通过，返回 200 |
| 匿名访问管理接口 | 通过，返回 401 |
| 管理员访问管理接口 | 通过，返回 200 |

当前服务地址：

- 工作台：`http://127.0.0.1:8000/`
- API 文档：`http://127.0.0.1:8000/docs`
- 就绪检查：`http://127.0.0.1:8000/api/v1/ready`

## 4. 本阶段涉及的主要文件

- `api_server.py`
- `main.py`
- `app/auth.py`
- `app/audit.py`
- `app/intent.py`
- `app/knowledge_base.py`
- `base/config.py`
- `rag_qa/core/rag_system.py`
- `rag_qa/core/vector_store.py`
- `case_rag/sparse.py`
- `case_rag/ingest.py`
- `case_rag/searcher.py`
- `web/index.html`
- `.env.example`
- `config.ini`
- `pyproject.toml`
- `uv.lock`
- `tests/unit/test_phase1_enterprise.py`
- `tests/integration/test_legal_rag_api_contract.py`
- `tests/integration/test_api_health.py`

## 5. 仍需在后续阶段完成的企业化工作

以下内容不应被误认为已经在第一阶段完成：

1. 将共享 Token 升级为律所统一身份认证，如 OIDC、OAuth 2.0、LDAP 或企业微信。
2. 将文档注册表和索引任务从本地 JSON、进程内 BackgroundTasks 迁移到 MySQL 与持久化任务队列。
3. 将单进程内存限流迁移到 Redis 分布式限流，以支持多实例部署。
4. 增加杀毒引擎、压缩炸弹检查、PDF/DOCX 深度解析安全和对象存储。
5. 建立知识库版本发布、审核、回滚、失效法条更新和案例增量更新流程。
6. 对旧案例集合执行一次完整重新入库，使历史稀疏向量使用稳定哈希算法。
7. 增加 RAG 评测集，量化 Recall@K、MRR、引用准确率、忠实度和拒答率。
8. 增加 Prometheus 指标、集中日志、告警规则、链路追踪和运行仪表盘。
9. 完成并发压测、故障注入、备份恢复演练、渗透测试和隐私影响评估。
10. 建立大模型数据出境、敏感信息脱敏、供应商协议和人工复核制度。

## 6. 第一阶段结论

第一阶段已经完成，项目当前具备企业应用的基础安全和运行控制，可以继续进入第二阶段工程化建设，也可以作为受控内网试用版本进行小范围验证。

它现在仍不是“无需任何额外工作即可面向全律所正式生产上线”的最终版本。达到正式企业生产标准，需要继续完成第 5 节列出的部署、运维、安全、评测和治理工作，并通过律所自身的信息安全与业务验收。
