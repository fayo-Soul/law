# 律所法律知识库 RAG 系统：第二阶段修改总结

## 1. 第二阶段目标

第二阶段在第一阶段安全基础上，重点解决单机原型的工程化问题：

- 文档元数据不能继续依赖本地 JSON 文件。
- 长时间索引任务不能依赖 Web 进程内后台任务。
- 限流不能只在单个 Python 进程中生效。
- 内部资料必须经过审核发布后才能进入检索。
- 会话 ID 不能由客户端任意伪造。
- 运行指标必须能被 Prometheus 直接采集。
- RAG 质量必须有可重复执行、能够阻断发布的量化门槛。

本阶段完成的是企业工程底座，不代表检索质量已经达到正式生产上线标准。离线评测已发现当前历史知识库的关键法条召回率不足，详见第 5 节。

## 2. 修改点、修改原因和技术点

### 2.1 文档注册表迁移到 MySQL

**修改点**

- 新增 `app/document_repository.py`。
- 新增 MySQL 表 `rag_documents`。
- 文档 ID、哈希、类型、处理状态、生命周期、访问级别、更新时间和完整 JSON 元数据统一存入 MySQL。
- 应用首次启动时自动读取 `data/knowledge_base/documents.json`，将尚未迁移的记录导入 MySQL。
- 测试环境继续支持 JSON Repository，避免单元测试依赖远程数据库。
- 文档写入使用事务和唯一哈希约束，删除使用明确的单记录删除。

**修改原因**

本地 JSON 文件不支持多实例共享、数据库约束、可靠事务和统一备份。多个服务实例同时写入时可能覆盖数据，文件损坏也会影响全部文档登记。

**技术点**

- Repository 模式。
- PyMySQL。
- InnoDB 事务。
- `file_hash` 唯一约束。
- JSON 字段保存完整元数据。
- 幂等建表与兼容迁移。

**成果**

VMware MySQL 已成功创建并使用 `rag_documents`。临时验收文档的创建、更新、查询和删除均通过 MySQL 完成，验收结束后数据库记录已清理。

### 2.2 Redis 持久化索引任务队列

**修改点**

- 新增 `app/task_queue.py`。
- 文档重建索引不再使用 FastAPI `BackgroundTasks`。
- 每个任务生成独立 `job_id`，状态包括 `queued`、`processing`、`retrying`、`completed` 和 `failed`。
- 使用 Redis List 保存待处理和处理中任务。
- 使用 Redis Hash 保存任务状态、错误和任务载荷。
- 服务重启时将遗留在 processing 队列的任务恢复到 ready 队列。
- 失败任务最多自动重试 3 次。
- 新增 `GET /api/v1/index-jobs/{job_id}` 管理接口。

**修改原因**

进程内任务在 Web 服务重启后会丢失，也无法跨实例消费、查询任务状态或恢复失败任务，不适合大型 PDF、DOCX 和批量资料索引。

**技术点**

- Redis `LPUSH`。
- Redis `BRPOPLPUSH`。
- ready/processing 双队列。
- Redis Hash 任务状态。
- 守护 Worker Thread。
- 至少一次处理语义。
- 失败重试和启动恢复。

**成果**

端到端验收任务最终状态为 `completed`，文档处理状态为 `indexed`，实际生成 1 个分块。Redis 队列深度和处理中任务数均可监控。

### 2.3 Redis 分布式限流

**修改点**

- 新增 `app/rate_limit.py`。
- 服务就绪后使用 Redis 计数代替单进程内存计数。
- 同一客户端的请求在多个 API 实例之间共享限流状态。
- Redis 不可用且应用尚未就绪时保留本地限流作为启动保护。

**修改原因**

原限流状态只存在一个 Python 进程中。部署多个 Uvicorn Worker 或多个应用实例后，每个实例都会独立计数，实际限制会被成倍放大。

**技术点**

- Redis Pipeline。
- `INCR` 原子计数。
- 固定时间窗口。
- Redis Key 自动过期。
- 多实例共享状态。

**成果**

单元测试验证两个不同限流器实例共享同一 Redis 计数，超过阈值后正确拒绝。

### 2.4 文档审核、发布和退役流程

**修改点**

- 文档新增 `lifecycle_status`。
- 生命周期为 `draft → pending_review → published → retired`。
- 支持待审核退回草稿。
- 非法状态跳转返回 HTTP 409。
- 只有 `published` 文档可以提交索引任务。
- 内部资料检索强制增加 `lifecycle_status=published` 过滤。
- 文档退役时删除对应 Milvus 向量。
- 发布操作记录审核人。
- 新增 `POST /api/v1/documents/{document_id}/lifecycle`。

**修改原因**

上传成功不代表资料已经经过律所审核。未经确认、已过期或存在错误的内部研究资料不能直接进入律师检索结果和大模型上下文。

**技术点**

- 有限状态机。
- 服务端状态跳转校验。
- Milvus 生命周期元数据过滤。
- 发布人与操作审计。
- 退役时向量清理。

**成果**

验收文档初始为 `draft`；草稿直接索引返回 409；提交审核和发布后才能入队；发布文档索引成功。

### 2.5 会话 ID 签名

**修改点**

- 会话 ID 由随机 UUID 和 HMAC-SHA256 签名组成。
- 客户端提交的会话 ID 必须通过签名校验。
- 会话 ID 被修改、截断或伪造时，系统创建新会话，不读取原会话内容。
- 用户数据删除接口拒绝操作非法会话 ID。
- 新增 `SESSION_SIGNING_KEY` 配置。

**修改原因**

仅使用客户端传入的 UUID 无法确认会话 ID 是否由系统签发。攻击者可能尝试猜测或替换 ID，访问其他会话。

**技术点**

- HMAC-SHA256。
- 常量时间签名比较。
- 服务器端签发。
- Redis TTL 会话存储。

**成果**

单元测试验证合法会话可使用，篡改后的会话 ID 不会打开原会话。

### 2.6 内部资料外发策略

**修改点**

- 新增 `ALLOW_INTERNAL_TO_EXTERNAL_LLM`，默认值为 `false`。
- 当问题要求使用内部资料，而配置的大模型地址是公网地址时，系统默认返回 403。
- 使用本地或私有网络大模型时允许处理内部资料。
- 律所明确完成供应商和数据合规审批后，管理员才可显式开启外发。

**修改原因**

内部研究资料虽然有检索权限控制，但如果检索结果被发送到外部大模型，仍可能形成数据出境或商业秘密泄露风险。

**技术点**

- URL Host 解析。
- 私有 IP 判断。
- 默认拒绝策略。
- HTTP 403 `POLICY_BLOCKED`。

**成果**

内部资料不会在无明确批准的情况下被发送到当前公网模型服务。

### 2.7 Prometheus 指标和真实运行诊断

**修改点**

- `/metrics` 从普通 JSON 改为 Prometheus 标准文本。
- 指标包括：
  - `law_rag_http_requests_total`
  - `law_rag_http_errors_total`
  - `law_rag_http_latency_milliseconds_sum`
  - `law_rag_index_queue_depth`
  - `law_rag_index_jobs_processing`
- `/api/v1/ready` 增加 `index_queue` 状态。
- 就绪检查现在覆盖 MySQL、Redis、Milvus、RAG 和索引队列。

**修改原因**

原 `/metrics` 虽然名为 Prometheus 指标，但返回的是普通 JSON，Prometheus 不能直接抓取；同时也看不到索引任务积压情况。

**技术点**

- Prometheus Exposition Format。
- Counter 和 Gauge。
- Redis 队列深度。
- 依赖就绪探针。

**成果**

实测 Prometheus 文本可正常访问，五项就绪状态均为 `true`。

### 2.8 结构化日志和日志轮换

**修改点**

- 重写 `base/logger.py`。
- 控制台保留便于开发阅读的文本格式。
- 文件日志改为 JSON Lines。
- 文件按天轮换，保留 30 天。
- 异常日志包含堆栈。
- API、检索、数据库和索引队列可使用统一日志初始化方式。

**修改原因**

普通拼接文本不利于日志平台解析，也缺少自动轮换，长期运行可能导致单个日志文件无限增长。

**技术点**

- 自定义 `logging.Formatter`。
- `TimedRotatingFileHandler`。
- UTC 时间。
- JSON Lines。
- 日志保留策略。

**成果**

结构化日志输出到 `logs/app.jsonl`，可被 Loki、ELK、OpenSearch 等平台采集。

### 2.9 RAG 离线评测和质量门槛

**修改点**

- 修复 `scripts/evaluate_rag.py` 中引用字段错误和未定义变量。
- Recall 匹配同时使用文档标题和条款位置。
- 支持命令行质量阈值。
- 报告新增引用覆盖率、质量门槛结果和总通过状态。
- 任一门槛不通过时进程退出码为 2，可阻断 CI/CD 发布。
- 生成验收报告 `data/eval/phase2_acceptance.json`。

**默认质量门槛**

- Recall@K 不低于 70%。
- 引用覆盖率不低于 90%。
- 平均响应耗时不超过 15 秒。
- 运行错误数为 0。

**修改原因**

只有“接口返回 200”不能证明 RAG 可用。法律系统必须量化关键依据是否被召回、回答是否有引用、延迟是否可接受。

**技术点**

- Golden Dataset。
- Recall@K。
- Citation Coverage。
- Latency Gate。
- 机器可读 JSON 报告。
- 非零退出码质量门禁。

**成果**

质量门槛已经能够真实识别检索问题，而不是无条件显示成功。

## 3. 自动化测试

执行命令：

```powershell
python -m pytest -q --basetemp .pytest-temp
```

结果：

```text
57 passed, 8 warnings
```

新增覆盖：

- 文档默认草稿状态。
- 审核和发布状态更新。
- Redis 多实例共享限流。
- HMAC 会话签名和篡改检测。
- 标题与条款联合 Recall 计算。
- Prometheus 标准文本契约。

8 条 warning 为第三方 `pkg_resources` 和 namespace package 弃用提示，不影响当前测试，但应在依赖升级任务中处理。

## 4. VMware 端到端验收

依赖地址：`192.168.88.100`

| 验收项 | 结果 |
| --- | --- |
| MySQL 文档注册表 | 通过 |
| Redis 分布式限流依赖 | 通过 |
| Redis 索引任务队列 | 通过 |
| Milvus 文档索引 | 通过 |
| RAG 初始化 | 通过 |
| 索引队列 Worker | 通过 |
| 未发布文档索引 | 正确拒绝，HTTP 409 |
| 草稿提交审核 | 通过 |
| 审核后发布 | 通过 |
| 发布后创建索引任务 | 通过 |
| 任务状态查询 | `completed` |
| 文档分块数量 | 1 |
| 测试文档和向量清理 | 通过 |
| Prometheus 指标 | 通过 |

最终 `/api/v1/ready`：

```json
{
  "mysql": true,
  "redis": true,
  "milvus": true,
  "rag": true,
  "index_queue": true
}
```

## 5. RAG 质量评测结果

本次使用 3 条样本进行链路验收：

| 指标 | 结果 |
| --- | --- |
| 成功执行 | 3/3 |
| 有引用回答 | 3/3 |
| Recall@K | 0% |
| 平均响应耗时 | 31,159 ms |

### 结论

**当前未通过默认质量门槛。**

问题不是评测程序故障。实际检查发现：

- 历史法规向量记录的标题存在乱码。
- 历史记录缺少可靠的法条位置、效力状态和文档类型元数据。
- “离婚后抚养权”和“违法解除劳动合同赔偿”没有召回评测集指定的关键条款。
- 平均 31 秒响应时间超过默认 15 秒门槛。

这说明旧知识库入库质量不足。第二阶段建立了可以发现并阻断该问题的质量门禁，但不能把 0% Recall 描述为已达到生产质量。

### 下一步必须整改

1. 重新解析法规原始数据，统一 UTF-8 编码。
2. 为每个法条补齐 `law_name`、`article_number`、效力状态、施行日期和版本。
3. 使用新元数据完整重建法规 Milvus Collection。
4. 对旧案例集合执行稳定哈希重新入库。
5. 扩大 Golden Dataset 并逐领域验收。
6. 调整召回数量、混合检索权重和 rerank 参数。
7. 缓存高频问题，压缩模型输入并评估更低延迟模型。
8. 使用默认门槛重新运行全部 55 条评测，必须通过后才能进入生产发布评审。

## 6. 主要修改文件

- `app/document_repository.py`
- `app/task_queue.py`
- `app/rate_limit.py`
- `app/knowledge_base.py`
- `api_server.py`
- `base/config.py`
- `base/session_manager.py`
- `base/logger.py`
- `main.py`
- `rag_qa/core/vector_store.py`
- `scripts/evaluate_rag.py`
- `tests/unit/test_phase2_enterprise.py`
- `tests/integration/test_api_integration.py`
- `.env.example`
- `config.ini`
- `README.md`
- `pyproject.toml`
- `uv.lock`
- `data/eval/phase2_acceptance.json`

## 7. 尚未完成的外部企业集成

以下事项依赖律所现有基础设施或管理制度，当前项目内不能虚构为已完成：

1. OIDC、LDAP、企业微信或律所统一身份平台接入。
2. 商用杀毒引擎、DLP 和文件内容安全平台接入。
3. Prometheus、Grafana、Loki 或 ELK 服务端部署及告警联系人配置。
4. 对象存储、异地备份和恢复演练。
5. 大模型供应商合同、数据处理协议和数据出境评估。
6. 律所知识审核人、发布人、应急联系人和权限审批制度。
7. 多实例压力测试、渗透测试和故障注入。

项目已预留标准接口和安全默认值，但这些外部能力必须由实际部署环境完成配置和验收。

## 8. 第二阶段结论

第二阶段工程改造已经完成：

- 文档元数据进入 MySQL。
- 索引任务进入 Redis 持久队列。
- 限流支持多实例。
- 文档必须审核发布。
- 会话 ID 可以验证真伪。
- 内部资料默认禁止发送外部模型。
- Prometheus 和结构化日志可用于运维。
- RAG 质量可以量化并阻断发布。

当前版本已经具备比第一阶段更完整的企业工程能力，可以继续用于受控内网试用和第三阶段数据质量整改。

但由于当前法规知识库 Recall@K 和响应延迟未通过质量门槛，**当前仍不应批准为正式生产上线版本**。下一阶段首要任务应是重建高质量法规索引并通过 55 条以上的全量质量验收，而不是继续增加表面功能。
